/**
 * what woudl I need for a ChatListItem
 * group/person, if person online status, img, name, last message, last message timestamp, number of unread messages, 
 * pinned, your last message read /no 
 * 
 */

export const ChatListItem = () => {
  return (

  )
}
